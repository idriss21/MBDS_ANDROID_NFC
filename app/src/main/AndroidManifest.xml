<?xml version="1.0" encoding="utf-8"?>
<manifest package="emsi.izouhair.com.mbdsnfc_tp"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:parentActivityName=".MainActivity" android:name=".LoginActivity"/>
        <activity android:parentActivityName=".MainActivity" android:name=".RegisterActivity"/>
        <activity android:parentActivityName=".MainActivity" android:name=".WelcomeActivity"/>



    </application>

</manifest>